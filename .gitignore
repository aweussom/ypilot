# Lokal Phaser-referanse (kildekode + docs + migrerings-skill) — kun for oppslag, ikke i repo
vendor/

# OS-skrot
.DS_Store
Thumbs.db
desktop.ini

# Skjermbilder / debug-bilder (droppes ikke i repo)
*Skjermbilde*.png

# Genererte kart-previews (regenereres med retrorocket/json_to_png.py)
retrorocket/png_preview/

# Opphavsrettede kilde-bilder for kart-gjenskaping (kartet lever i maps-ascii/maps-json;
# selve tegningen redistribueres IKKE i det offentlige repoet). F.eks. Calvin & Hobbes.
calvin-hobbes-dancing.png
